# Client

React frontend for Dynamic Engine. See the [root README](../README.md) for
architecture and setup.

```bash
npm install
npm run dev      # http://localhost:5173
```

Requires the server running on port 4000 — Vite proxies `/api` to it.
